# Pflicht
numpy>=1.26
pillow>=10.0

# Transkription mit Wort-Timecodes — eins von beiden:
#   Apple Silicon (schnellster Weg, nutzt Metal):
mlx-whisper>=0.4 ; sys_platform == "darwin" and platform_machine == "arm64"
#   ueberall sonst (und als Rueckfalloption auf dem Mac):
faster-whisper>=1.0

# Analyse fuer 'learn'
opencv-python>=4.9
scenedetect>=0.6.4
pytesseract>=0.3.10          # braucht das Programm tesseract (brew install tesseract tesseract-lang)
# easyocr>=1.7               # Alternative zu tesseract, genauere Kaesten, deutlich groesser

# Overlays als HTML/CSS
playwright>=1.44

# nur fuer 'stock' (Pexels)
requests>=2.31
